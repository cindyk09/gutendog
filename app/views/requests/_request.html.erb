<h4>Pets</h4>
<ul>
  <% request.owner.pets.each do |pet| %>
    <li><%= pet.name %></li>
  <% end %>
</ul>

<strong>Start Time:</strong><%= request.start_time.to_s  %>

<strong>End Time:</strong><%= request.end_time.to_s  %>

<% if request.pickup %>
  <p>
    You need to pickup the pups!
  </p>
<% else %>
  <p>
    <%= request.owner.first_name %> will drop off the pups at your home.
  </p>
<% end %>
<% if request.active? %>
  <div class="accept-request">
    <%= button_to "Accept This Walk Request!", walks_path, params: {request_id: request.id} %>
  </div>
<% else %>
  <p><%= request.walk.walker.first_name %> will walk the pups</p>
<% end %>
