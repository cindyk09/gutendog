<div class="col-sm-4">
  <div class="image-box team-member white-bg">
    <%= link_to image_tag(pet.image_url(:big_thumb)), pet_path(pet) %>
    <div class="image-box-body">
      <h3 class="title text-center"><%= link_to pet.name, pet_path(pet) %></h3>
    </div>
  </div>
</div>
